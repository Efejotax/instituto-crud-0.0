
<nav class="lg:h-nav bg-nav flex flex-col lg:flex-row
px-5 justify-start items-center lg:space-x-2">
    <a href="{{route("main")}}" class="btn btn-glass w-full lg:w-auto">Main</a>
    <a href="{{route("home")}}" class="btn btn-glass w-full lg:w-auto">Home</a>
    <a href="{{route("welcome")}}" class="btn btn-glass w-full lg:w-auto">Welcome</a>
    <a href="{{route("about")}}" class="btn btn-glass w-full lg:w-auto">About</a>
    <a href="{{route("noticias")}}" class="btn btn-glass w-full lg:w-auto">Noticias</a>
    <a href="{{route("dashboard")}}" class="btn btn-glass w-full lg:w-auto">Dashboard</a>
    <a href="{{route("alumnos")}}" class="btn btn-glass w-full lg:w-auto">Alumnos</a>
    <a href="{{route("alumno")}}" class="btn btn-glass w-full lg:w-auto">Alumno</a>
    <a href="{{route("profesores")}}" class="btn btn-glass w-full lg:w-auto">Profesores</a>
    <a href="{{route("profesor")}}" class="btn btn-glass w-full lg:w-auto">Profesor</a>
    <a href="{{route("contacto")}}" class="btn btn-glass w-full lg:w-auto">Contacto</a>
    <a href="{{route("sumar")}}" class="btn btn-glass w-full lg:w-auto">Sumar</a>
    <a href="{{route("restar")}}" class="btn btn-glass w-full lg:w-auto">Restar</a>
    <a href="{{route("multiplicar")}}" class="btn btn-glass w-full lg:w-auto">Multiplicar</a>
    <a href="{{route("dividir")}}" class="btn btn-glass w-full lg:w-auto">Dividir</a>
    <a href="{{route("potenciar")}}" class="btn btn-glass w-full lg:w-auto">Potenciar</a>
    <a href="{{route("raizcuadrada")}}" class="btn btn-glass w-full lg:w-auto">Raiz2</a>
    <a href="{{route("products")}}" class="btn btn-glass w-full lg:w-auto">Productos</a>
    <a href="{{route("projects")}}" class="btn btn-glass w-full lg:w-auto">Proyectos</a>
    <a href="{{route("pokemon.index")}}" class="btn btn-glass w-full lg:w-auto">Pokemon</a>
</nav>






